[tox]
envlist = 
    py39-django42
    py310-django50
    py311-django50

[testenv]
deps =
    pytest
    pytest-django
    django>=4.2
    # Optional: include other test deps here
commands = pytest
setenv =
    DJANGO_SETTINGS_MODULE=tests.settings
    PYTHONPATH={toxinidir}
